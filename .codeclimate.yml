engines:
  eslint:
    enabled: true
    channel: "eslint-8"
    config:
      config: ".eslintrc.yaml"

ratings:
   paths:
   - "**.js"

checks:
  method-lines:
    config:
      threshold: 30
  method-complexity:
    config:
      threshold: 10
  return-statements:
    enabled: false